/**
 * 通用像素渲染组件的公开入口。
 *
 * 这里只导出不依赖具体页面业务的基础组件。
 */

export { default as PixelImage } from './PixelImage.vue'
export { default as PixelPattern } from './PixelPattern.vue'
export { default as PixelText } from './PixelText.vue'